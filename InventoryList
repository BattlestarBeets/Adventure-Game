#include <iostream>
using std::cout; using std::cin; using std::string; using std::endl;

void :: inventory::getInventory()
{
	player* p1 = player::getPlayer();

	int i = 0;

	while (i < playerInventory; i++)
	{
		cout << playerInventory(i) << endl;
	}

	int i = 0;
}
